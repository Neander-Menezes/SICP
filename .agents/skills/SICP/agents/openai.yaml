interface:
  display_name: "SICP"
  short_description: "Repo-specific patterns and workflows for SICP"
  default_prompt: "Use the SICP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true